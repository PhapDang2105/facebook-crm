# Assets

- `branding/logos/`: logo dùng trong giao diện (`giot-nang-logo.webp`) và icon shortcut (`facebook-logo-large.ico`).
- `templates/facebook-order-export.xlsx`: mẫu file xuất kho, do `scripts/setup/build-order-export-template.py` dựng.

Icon và logo nhà mạng mà giao diện tải trực tiếp nằm trong `web/assets/`.
