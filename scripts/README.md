# Scripts

- `import/`: tiếp nhận dữ liệu.
- `cleaning/`: chuẩn hóa và khử trùng.
- `reconciliation/`: đối soát nguồn, CRM và Meta.
- `maintenance/`: backup, retention và tác vụ định kỳ.

Script phải hỗ trợ dry-run, log số lượng xử lý và chạy lại an toàn khi có thể.

